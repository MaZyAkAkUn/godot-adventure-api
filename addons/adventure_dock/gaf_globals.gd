#
#Store all the global variables & functions that may be reused
#This script has to in root (due to references to it)
#In other scripts, you'll use it as g.member
#
extends Control

onready var path_selector = get_node('path_selector')

func _ready():
	pass


